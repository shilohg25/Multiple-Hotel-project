import { requireStaff } from '@/lib/auth';export default async function P(){await requireStaff();return <div className='space-y-3'><h1 className='text-3xl font-black'>New Day Tour Booking</h1><p className='text-sm text-slate-600'>Payment proof is required when status is payment_submitted. Secured bookings count against capacity; tentative/payment_submitted remain visible but do not block capacity.</p></div>}
