import { redirect } from 'next/navigation';import { requireStaff, canManagePricingSetup } from '@/lib/auth';import { supabaseAdmin } from '@/lib/supabase-admin';
const examples=['breakfast','extra bed','chauffeur','late checkout','early check-in','laundry','corkage','damage charge','other'];
export default async function P(){const staff=await requireStaff();if(!canManagePricingSetup(staff.profile)) redirect('/dashboard');const q=supabaseAdmin.from('service_items').select('id,name,category,default_price,active').order('name');if(staff.profile.role!=='owner'&&staff.profile.hotel_id) q.eq('hotel_id',staff.profile.hotel_id);const {data}=await q;return <div className='space-y-5'><h1 className='text-3xl font-black'>Charges</h1><p className='text-sm text-slate-600'>Examples: {examples.join(', ')}.</p><div className='card p-4'><ul className='space-y-2'>{(data||[]).map((i)=><li key={i.id}>{i.name} · {i.category} · ₱{Number(i.default_price).toFixed(2)} {i.active?'':'(inactive)'}</li>)}</ul></div></div>}
